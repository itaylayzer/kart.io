// server codes
export enum CS {
    JOIN = "j",
    UPDATE = "u",
    KEY_DOWN = "d",
    KEY_UP = "u",
}

// client codes
export enum CC {
    INIT = "i",
    NEW_PLAYER = "p",
    UPDATE = "u",
    DISCONNECTED = "d",
}
