import { GiCartwheel } from "react-icons/gi";
import { IconType } from "react-icons/lib";

export const LoadingWheelSVG: IconType = (props) => {
    return <GiCartwheel className="animate-rotate" {...props} />;
};
